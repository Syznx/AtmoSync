SELECT
    container_id,
    commodity,
    origin,
    destination,
    current_location,
    latitude,
    longitude,
    temperature_c,
    humidity_pct,
    vibration,
    spoilage_pct,
    reroute_needed,
    recommended_market,
    reroute_reason,
    base_price_per_kg,
    recommended_price_kg,
    timestamp,
    health_status
FROM {{ ref('stg_iot_events') }}
